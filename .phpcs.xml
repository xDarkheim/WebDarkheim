<?xml version="1.0"?>
<ruleset name="Disabled PHPCS">
    <description>Completely disable PHPCS</description>

    <!-- Disable all rules -->
    <exclude-pattern>*</exclude-pattern>

    <!-- No rules enabled -->
</ruleset>

